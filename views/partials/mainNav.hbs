<div id="nav-container">
    <div id="logo">
        <a class="logo" href="/"><i id="imagelogo" class="fas fa-plane-departure"></i></a>
        <h2 class="logo-item"><a href="/">Fly with me</a> </h2>
    </div>

    <div id="nav">
        <ul class="Nav">
            {{#unless isLoggedIn}}
            <li>
                <p><a href="#">Passenger</a> </p>
                <div>
                    <a href="/signin/user">Sign in</a> <a href="/signup/user">Sign up</a>
                </div>
            </li>
            {{/unless}}
            {{#unless isLoggedIn}}
            <li>
                <p><a href="#">Pilot</a></p>
                <div>
                    <a href="/signin/pilot">Sign in</a> <a href="/signup/pilot">Sign up</a>
                </div>
            </li>
            {{/unless}}
        </ul>
        {{#if isLoggedIn}}
       {{#unless isPilotUser}}
        <li class="nav-item">
            <a id="navA" href="/search">Search for a flight</a>
        </li>
        {{/unless}}
        {{/if}}
        {{#if isPilotUser}}
        <li class="nav-item">
            <a id="navA" href="/add">Add a flight</a>
        </li>
        {{/if}}
        {{#if isLoggedIn}}
        <a id="navA" href="/logout" class="nav-item">
            <span class="btn-text">Log out</span>
        </a>
        {{/if}}
        </ul>
    </div>
</div>
<hr>